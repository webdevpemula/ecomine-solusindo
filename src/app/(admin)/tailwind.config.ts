import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: '#1E3A8A', // Biru tambang
        secondary: '#065F46', // Hijau lingkungan
      },
    },
  },
}

export default config